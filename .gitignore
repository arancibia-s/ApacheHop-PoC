# Nunca commitear ni hornear credenciales reales
.env
.env.*
!.env.*.example

# Carpetas
apache-hop-client*/

# Invoices de muestra usados para testear localmente - no subir al repo
hop-mvp/ETLs/Cruises/Invoices/*.xml